// 임의로 작성한 데이터 클래스입니다
// 프론트 테스트를 위해 필요한 정보만 담았습니다
// api에 따라 수정하겠습니다

class Coordi {
  final String image;
  final String posterName;
  final String posterId;
  final List<String> style;
  final String weather;

  Coordi(this.image, this.posterName, this.posterId, this.style, this.weather);
}